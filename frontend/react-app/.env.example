# Local development: backend runs on port 8000 (uvicorn ... --port 8000)
# For Docker Compose this is overridden to "" (empty) so the browser uses a
# same-origin relative path (/api/...) which nginx proxies to the backend.
VITE_API_BASE_URL=http://localhost:8000
